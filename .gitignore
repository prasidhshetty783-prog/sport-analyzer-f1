# data + artifacts (rebuildable / large)
data/
models/artifacts/
ml/reports/*.html
ml/reports/*.png

# secrets
.env

# python
__pycache__/
*.pyc
.venv/
.pytest_cache/
*.egg-info/

# node (later phases)
node_modules/
frontend/dist/

# large local reference assets (NOT part of the app — 2+ GB of raw 3D sources;
# the app only uses an optional small frontend/public/models/f1.glb)
F1-2022-Monopost-3D-Model/
*.blend
*.c4d
*.obj
*.fbx

# raw / demo video — too big for GitHub (100MB hard limit). Host the full demo on
# YouTube, or drag a small compressed clip into the README editor (GitHub's CDN
# hosts it) instead of committing video to the repo.
*.mp4
*.mov
*.webm

# internal build / collaboration docs — kept locally, not published to the repo
CLAUDE.md
BUILD_PROMPT.md
CONVERSATION_SUMMARY.md
LINKEDIN_POST.md
vault/

# personal scratch notes
Great work with adding elevation al.txt

# misc
*.log
.DS_Store
nohup.out
